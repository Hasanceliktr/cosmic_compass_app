// Bu dosya, backend'deki User entity'sinde kullanılan enum'ları içerir.

enum Gender {
  MALE,
  FEMALE,
  OTHER
}

enum RelationshipStatus {
  SINGLE,
  IN_RELATIONSHIP,
  MARRIED,
  DIVORCED,
  WIDOWED,
  ITS_COMPLICATED
}